{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "weareleba",
      "runtimeExecutable": "python3",
      "runtimeArgs": ["-c", "import os,http.server,functools; http.server.test(HandlerClass=functools.partial(http.server.SimpleHTTPRequestHandler,directory='.'), port=int(os.environ.get('PORT',8000)))"],
      "port": 4324,
      "autoPort": true
    }
  ]
}
